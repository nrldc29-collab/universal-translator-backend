# Railway Production Environment Variables
# Copy these to Railway project settings after deployment.
# Generate secrets: python scripts/generate_secrets.py

# Runtime
ENVIRONMENT=production
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
SERVE_FRONTEND_DIST=1
FRONTEND_DIST_DIR=frontend/dist
EVENT_LOG_PATH=logs/events.jsonl
WORKERS=1

# Auth and quotas - PRODUCTION REQUIRED
# Replace JWT_SECRET with output from: python scripts/generate_secrets.py
JWT_SECRET=<GENERATE_WITH_scripts/generate_secrets.py>
USERS=admin:ChangeThisPassword123
USER_TIERS=admin:standard
API_KEYS=
ADMIN_IDENTITIES=admin
REQUESTS_PER_MINUTE=30
QUOTA_REQUESTS_PER_HOUR=200
FREE_DAILY_AUDIO_MINUTES=30
SESSION_MINUTES=480

# CORS - PRODUCTION REQUIRED
# Replace with your actual Railway service URL (Networking tab in Railway dashboard)
ALLOWED_ORIGINS=https://<your-service>.up.railway.app
ALLOWED_ORIGIN_REGEX=https?://<your-service>\.up\.railway\.app

# Speech-to-text
USE_GPU=0
GPU_COST_MODE=low
GPU_MEMORY_USED_MB=0
GPU_UTILIZATION_PERCENT=0
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8
WHISPER_MODEL_SIZE=tiny
WHISPER_CPU_THREADS=4
WHISPER_NUM_WORKERS=1
WHISPER_BEAM_SIZE=1
STT_MAX_CONCURRENCY=2
STT_QUEUE_MAX_DEPTH=8

# Translation
TRANSLATION_BACKEND=hybrid
TRANSLATION_DEVICE=cpu
REMOTE_TRANSLATION_TIMEOUT_SECONDS=0.65
# Marian fallback loads extra models — keep 0 on Railway CPU for faster startup
HYBRID_ENABLE_MARIAN_FALLBACK=0
CIP_MODE=ut_first
CIP_DEFAULT_MODE=ut_first
CIP_PROCESS_URL=
CIP_TIMEOUT_SECONDS=0.65
CIP_RETRIES=0
CIP_CONFIDENCE_THRESHOLD=0.42
CIP_AMBIGUITY_THRESHOLD=0.68

# AILang (degrades gracefully without OPENAI_API_KEY)
AILANG_ENABLED=1
AILANG_AGENT_TIMEOUT=10
AILANG_CACHE_TTL=300
AILANG_CIRCUIT_FAILURE_THRESHOLD=5
AILANG_CIRCUIT_RECOVERY_TIMEOUT=60
AILANG_MAX_RETRIES=2
OPENAI_API_KEY=<OPTIONAL_OPENAI_KEY>
OLLAMA_ENABLED=0

# Streaming and VAD
MAX_AUDIO_MB=25
MAX_AUDIO_SECONDS=300
CLIENT_VAD_MODE=1
CLIENT_VAD_THRESHOLD=0.055
VAD_RECENT_CHUNKS=2
VAD_SILENT_CHECKS=1
VAD_FORCE_FINAL_SECONDS=0.25
SPEECH_MERGE_MS=40
MIN_SPEECH_BYTES=4000
PARTIAL_STT_MIN_BYTES=1200
PARTIAL_STT_INTERVAL_MS=100
PARTIAL_TRANSLATION_MIN_WORDS=1
PARTIAL_TTS_MODE=1
PARTIAL_TTS_MIN_INTERVAL=0.45
PARTIAL_TTS_MIN_WORDS=1
PARTIAL_TTS_MAX_WORDS=15
NEAR_ZERO_LATENCY_MODE=1
STREAM_BUFFER_MAX_MB=12
MAX_ACTIVE_STREAMS_PER_USER=2
PIPELINE_STEP_TIMEOUT_SECONDS=10
STREAM_HOT_PATH_LOGGING=0

# Predictive translation cache
PREDICTIVE_CACHE_SIZE=1000
PREDICTIVE_CACHE_TTL=3600

# TTS
TTS_CHUNK_CHARS=14
TTS_FIRST_CHUNK_CHARS=10
PREFER_CLOUD_TTS=1
GOOGLE_TTS_API_KEY=<OPTIONAL_GOOGLE_TTS_KEY>
ELEVENLABS_API_KEY=

# Model loading
PRELOAD_MODELS=1

# Persistence - mount Railway volume at /app/data
DATA_DIR=/app/data

# Session and memory limits
SESSION_TTL_SECONDS=1800
SESSION_HISTORY_LIMIT=20
SEMANTIC_HISTORY_LIMIT=12
TOPIC_LIMIT=25

# STT Provider
STT_PROVIDER=local
STT_PROVIDER_URL=
STT_PROVIDER_WS_URL=
STT_PROVIDER_API_KEY=
STT_API_KEY=
ADMIN_API_KEY=
