# Pinned to the versions validated by the passing test suite (Python 3.12).
# Regenerate after intentional upgrades: python -m pip freeze.
fastapi==0.136.1
uvicorn[standard]==0.34.0
pydantic==2.13.4
faster-whisper==1.1.0
transformers==5.9.0
torch==2.12.0
sentencepiece==0.2.1
# piper-tts is an optional runtime TTS backend (not installed in the test env).
piper-tts>=1.4.2
python-multipart==0.0.29
torchaudio==2.11.0
requests==2.34.2
websockets==14.1
pytest==9.0.3
# NAIA assistant integration (bundled under naia/)
anthropic==0.103.1
httpx==0.27.2
numpy==2.2.1
slowapi==0.1.9
# AILang advanced-translation agents (vendored ailang/ + ailang_integration/).
# openai is the agent LLM provider (used when OPENAI_API_KEY is set).
flask==3.1.3
flask-cors==6.0.2
openai==2.38.0
