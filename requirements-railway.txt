fastapi>=0.137.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
faster-whisper>=1.0.0
transformers>=4.38.0,<5
numpy<2
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchaudio==2.6.0+cpu
soundfile>=0.12.1
sentencepiece>=0.2.0
sacremoses>=0.1.1
python-multipart>=0.0.9
piper-tts>=1.2.0
requests>=2.31.0
# Required by backend/api.py (rate limiting) and backend streaming/STT client.
# Must stay in sync with requirements.txt or the Railway image crashes on startup.
slowapi>=0.1.9
websockets>=14.1
# Audio enhancement (noise reduction / AGC) in streaming hot path.
scipy>=1.11.0
# Free cloud TTS fallback when Piper voices are unavailable.
edge-tts>=7.2.0
# AILang advanced-translation agents (vendored ailang/ + ailang_integration/).
# Agents call OpenAI when OPENAI_API_KEY is set; otherwise they degrade gracefully.
flask>=3.0
flask-cors>=4.0
openai>=1.40
