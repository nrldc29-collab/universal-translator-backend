# Archive

Legacy scripts and superseded mobile/deployment artifacts live here.

Archive content is kept for reference only and is not part of the production import path. Prefer active directories such as `backend/`, `frontend/`, and `translator-mobile/` for new work.
