# LLM Context Layer

Optional context-improvement layer.

`context_layer.py` currently provides a passthrough implementation so the production pipeline stays local and predictable. Replace or extend this package for local LLM enhancement if needed.
