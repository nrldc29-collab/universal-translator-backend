[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# Run plain `async def test_*` coroutines without per-test markers, and let the
# pytest-asyncio plugin (pinned in requirements.txt) drive them.
asyncio_mode = auto
